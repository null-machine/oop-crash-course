// interfaces are like abstract classes, except inheritors can
// inherit from more than one interface
interface Interactable {
	
	// methods in an interface are public and abstract
	public abstract void interact();
}
